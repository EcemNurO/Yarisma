@model Yarışma.Models.Contestant

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Contestant</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ContestantProfilId" class="control-label"></label>
                <select asp-for="ContestantProfilId" class="form-control" asp-items="ViewBag.ContestantProfilId"></select>
                <span asp-validation-for="ContestantProfilId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ContestantCategoryId" class="control-label"></label>
                <select asp-for="ContestantCategoryId" class="form-control" asp-items="ViewBag.ContestantCategoryId"></select>
                <span asp-validation-for="ContestantCategoryId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
